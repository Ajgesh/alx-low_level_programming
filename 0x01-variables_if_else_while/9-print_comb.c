#include <stdio.h>

/**
 * main - Entry point
 *
 * return: always success 0
 */
int main(void)
{
	int i;

	for (i = 10; i < 20; i++)
	{
		putchar ((i % 10) + '0');
		if (; != 19)
		{
			putchar (', ');
			putchar ('');
		}
	}
	putchar ('\n);
	return (0);
}
